# 이 내용을 복사해서 프로젝트 루트에 ".env" 파일로 저장하세요 (Vercel에서는 대시보드의 Environment Variables에 등록).

# Neon(또는 다른 Postgres) 연결 문자열. Vercel Postgres/Neon 대시보드에서 복사.
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# 세션 쿠키 서명용 비밀키. 아래 명령으로 생성해서 넣으세요:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET="change-me-to-a-random-64-char-hex-string"

# 진행 설정(마감/공개)을 바꿀 수 있는 관리자(실장)의 member id. lib/org.ts 참고.
# 기본 명단 기준 손석우(실장) = "m01"
ADMIN_MEMBER_ID="m01"
